# Front Start

Проект для изучения Frontend технологий с интерактивной базой знаний по JavaScript и Browser API.

## Описание

**Front Start** — это образовательный проект для подготовки к техническим интервью на позицию front-end разработчика. Проект содержит структурированную базу знаний с примерами кода, объяснениями и визуализаторами для лучшего понимания концепций.

Темы, представленные в проекте, — это те вопросы и концепции, которые чаще всего встречаются в технических интервью на вакансии front-end разработчика. Каждая мета-секция содержит подразделы, сгруппированные по смыслу тем. Темы также разделены по уровню сложности: **beginner** (базовый), **intermediate** (средний) и **advanced** (продвинутый).

Каждая тема может быть помечена как **front-end-essential**, что означает, что эта тема обязательна для понимания и часто является ключевой на технических интервью.

## Критерии и требования к написанию контента

### Структура темы

Каждая тема должна следовать иерархии: **Description + KeyPoints → Examples**

**Опционально:** `funFact` (строка | массив), `additionalDescription`

### Description

- ✅ 2–3 предложения, без примеров кода
- ✅ Отвечает на «Что это?» и «Зачем?»
- ✅ Разбивать через `\n\n` если >100 слов
- ❌ Не дублировать KeyPoints/funFact
- ❌ Не использовать код в описании

### KeyPoints

- ✅ Структурированный список аспектов
- ✅ Каждый пункт = отдельная концепция
- ✅ Краткие уточнения в скобках допустимы
- ❌ Не перечисление фактов, не дублировать Description

**Пример:** `- Арифметические: +, -, *, /, %, **.`

### Examples

- ✅ Детальные примеры с кодом и комментариями
- ✅ Каждый example = одна идея
- ✅ Экранирование в шаблонных строках: `${{` → `\${{`, `${` → `\${`, `` ` `` → `\``, `\` → `\\`

### Title

- ✅ 2–4 слова (макс. 5), понятное и нейтральное
- ❌ Без пояснений и подзаголовков
- ❌ Не дублировать название раздела/категории в названии темы

**Хорошо:** `История JavaScript`, `Типизация`, `React компоненты`, `Пропсы и state`  
**Плохо:** `История JavaScript: как и зачем он появился`, `Типизация React компонентов` (если раздел уже называется "Типизация для React")

### Опциональные поля

**funFact:** интересный/исторический факт (1–3 предложения)
- Может быть строкой или массивом
- Первый факт → перед KeyPoints, остальные → после KeyPoints
- Не дублирует description/keyPoints

**additionalDescription:** расширяет тему после KeyPoints, не повторяет Description

### Принципы написания

- От общего к частному
- KeyPoints = справочник, Examples = практика
- История/факты → funFact
- Минимум дублирования между секциями

### Антипаттерны

❌ Код в Description  
❌ Длинные описания с перечислениями  
❌ KeyPoints без структуры  
❌ Дублирование между секциями  
❌ Смешивание концепций в одном пункте

### Структура Topic

```typescript
{
  id: 'unique-id',
  title: 'Название',
  difficulty: 'beginner' | 'intermediate' | 'advanced',
  description: 'Краткое описание без примеров',
  keyPoints: ['Каждый пункт = отдельная идея'],
  funFact?: 'Один факт' | ['Факт 1', 'Факт 2'],
  additionalDescription?: 'Дополнение',
  tags: ['tag1', 'tag2'],
  examples: [{ title: 'Пример', code: '// код' }],
  relatedTopics: ['related-topic-id'],
  isFrontendEssential?: boolean
}
```

### Теги

Использовать 5–10 тегов на тему. Примеры категорий:

- **Общие:** `basics`, `intermediate`, `advanced`, `history`, `fundamentals`, `engineering`
- **JavaScript:** `arrays`, `objects`, `types`, `functions`, `operators`, `scope`, `closure`, `async`, `promise`, `event-loop`
- **TypeScript:** `typescript`, `types`, `generics`, `utilities`, `interfaces`
- **Производительность:** `performance`, `optimization`, `memory`, `garbage-collection`, `caching`
- **Инструменты:** `tools`, `git`, `npm`, `docker`, `ci-cd`, `devtools`

### Чеклист перед добавлением темы

- [ ] Title 2–4 слова
- [ ] Description без примеров
- [ ] KeyPoints структурированы
- [ ] Examples с комментариями
- [ ] funFact не дублирует текст
- [ ] Нет повторов между секциями
- [ ] 5–10 тегов, 2–4 relatedTopics
- [ ] Помечена как `isFrontendEssential: true` если критична для собеседований

### Требования к заполнению разделов

1. **Уровни сложности:** Темы должны быть разделены на `beginner`, `intermediate`, `advanced`
2. **Приоритет тем:** Критичные для собеседований темы помечаются `isFrontendEssential: true`
3. **Связанные темы:** Каждая тема должна иметь 2–4 связанные темы в `relatedTopics`
4. **Полнота:** Каждая категория должна покрывать тему от базового до продвинутого уровня
5. **Актуальность:** Фокус на темах, которые спрашивают на технических интервью

Подробные правила: [`core/CONTENT_GUIDELINES.md`](core/CONTENT_GUIDELINES.md)

### Текущее состояние

**JavaScript-based Browser API:**
- Полное покрытие Browser API: DOM, Fetch, Events, Storage, Web Workers, Service Workers и другие
- Интерактивные визуализаторы для сложных концепций (например, Scope Chain)
- Структурированные темы от базового до продвинутого уровня
- Подсветка синтаксиса кода с помощью Prism.js
- Адаптивный интерфейс с поддержкой мобильных устройств

### В планах

- **Reactive frameworks и libraries**: разделы по React, Vue, Angular и другим фреймворкам
- **CSS-layer**: глубокое погружение в CSS, включая современные возможности (Grid, Flexbox, Custom Properties, Animations и т.д.)
- **Архитектурные паттерны**: MVC, MVP, MVVM, Flux, Redux, Clean Architecture и другие подходы к организации кода

## Структура проекта

Проект организован по категориям знаний:

- **Основы JavaScript**: типы данных, операторы, функции, массивы, объекты
- **Переменные и область видимости**: var/let/const, hoisting, TDZ, scope chain
- **Замыкания и окружение**: lexical environment, closures, IIFE
- **Контекст this**: основы, стрелочные функции, bind/call/apply
- **Прототипы и ООП**: prototype chain, конструкторы, классы
- **Продвинутый JavaScript**: Event Loop, Promises, async/await, генераторы
- **Функциональные концепции**: immutability, HOF, рекурсия, каррирование
- **Коллекции**: Map, Set, WeakMap, WeakSet, продвинутые методы массивов
- **ООП и модули**: классы ES6, модули, Symbol, паттерны проектирования
- **Продвинутые возможности**: деструктуризация, обработка ошибок, Proxy/Reflect, итераторы
- **Browser API**: полный набор браузерных API для работы с DOM, сетью, хранилищем и т.д.

## Технологический стек

- **React 19** — UI библиотека
- **TypeScript** — типизация
- **Vite** — сборщик и dev-сервер
- **Zustand** — state management
- **Tailwind CSS** — стилизация
- **Prism.js** — подсветка синтаксиса

## Запуск проекта

**Требования:** Node.js

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Запустите dev-сервер:
   ```bash
   npm run dev
   ```

3. Соберите production версию:
   ```bash
   npm run build
   ```

## Особенности

- 📚 Структурированная база знаний с категориями и темами
- 🎨 Современный темный UI с адаптивным дизайном
- 💡 Интерактивные визуализаторы для сложных концепций
- 🔍 Поиск и фильтрация тем
- 📱 Поддержка мобильных устройств
- 🎯 Примеры кода с подсветкой синтаксиса
- 🔗 Связанные темы для навигации

